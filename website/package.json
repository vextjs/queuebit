{
  "name": "queuebit-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rspress preview --port 4181 --host 127.0.0.1",
    "edit": "rspress dev --port 4182 --host 127.0.0.1",
    "build": "rspress build",
    "preview": "rspress preview --port 4180 --host 127.0.0.1",
    "validate:scenarios": "node scripts/validate-scenarios.mjs",
    "validate:links": "node scripts/validate-built-links.mjs"
  },
  "devDependencies": {
    "@rspress/core": "^2.0.15",
    "@types/node": "^22.15.17",
    "rspress-plugin-mermaid": "^1.0.1",
    "typescript": "^5.8.3"
  }
}
