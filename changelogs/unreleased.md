# Unreleased

## 2026-07-24

- **Runtime M2K Completion retention window**: added independent `retention.completionEvents.ageMs/maxCount` config with default parity to `terminalRuns`, a Redis Completion detail index maintained by Run cancellation and Coordinator completion creation paths, `retention.plan()` / `retention.purge()` completion-window logic based on that detail index, and targeted tests proving age/maxCount tombstones stop consuming the detail window while `completions.list/get` retain tombstone identity through the stable event index.
- **Docs implementation consistency repair**: aligned `docs/v01` public configuration, runtime helper, BatchRun source/mapper, CLI drain, public API, and error-code references with the current implementation; added a target-contract `examples/receipt-batch-vext` skeleton; expanded `docs:validate` to check source-backed public-contract drift; and registered `QB_INTERNAL` in the public error-code union used by CLI fallback JSON.
- **Docs full consistency rereview**: deepened the source-backed docs validator and user manual copy for exact Quick Start CLI output, vext `runs.start()` usage, Redis keyspace/destructive-operation boundaries, Worker `leaseGeneration` settle fencing, operations capacity/completion-retention anchors, deployment evidence environment variables, and homepage route parity.

## 2026-07-23

- **Runtime M0A foundation**: added the package build surface, public error model, configuration validation, canonical input digest helpers, and Node built-in tests for the first runtime development batch.
- **Runtime M0B Redis foundation**: added official `@redis/client` connection option mapping, Redis connection wrapper, server preflight checks, key namespace builder, script registry skeleton, Redis error codes, and Node built-in tests.
- **Runtime M0C jobs kernel**: added direct jobs public types, `createQueuebitJobsApi`, Lua script execution, atomic `jobs.add/addBulk`, `jobs.get/list`, hard bulk/high-watermark checks, dedupe conflict handling, stable list cursors, and Node built-in tests.
- **Runtime M0D Worker kernel**: added `createQueuebitWorkerKernel`, Redis Lua claim/renew/settle scripts, workerId plus leaseGeneration fencing, complete/fail settlement, job result size guards, internal waiting/due jobKey members for atomic claims, and Node built-in tests for multi-Worker ownership conflicts.
- **Runtime M0E Worker runner**: added `createQueuebitWorker`, start/drain/stop/status, concurrency fill loop, processor invocation, renew timers, AbortSignal timeout boundary, and lifecycle tests.
- **Runtime M0F cooperative advancement**: added due promotion, retry/backoff transition to `retrying`, stalled active job recovery, max stalled terminal failure, and Worker pump integration.
- **Runtime M0G direct job controls**: added atomic `jobs.cancel`, `jobs.retryFailed` replacement, `parentJobId` provenance, direct failed-job guard, and control API tests.
- **Runtime M0H client facade**: added `createQueuebitClient`, jobs/runs facade wiring, Worker factory, health snapshot, metrics rendering, and close/drain behavior.
- **Runtime M1A Run identity**: added Run state types, `createQueuebitRunsApi`, atomic `runs.start`, idempotency conflict detection, `runs.get/list`, stable run cursors, and Run contract tests.
- **Runtime M1B Coordinator dispatch foundation**: added `batchRuns` config validation, runtime source/mapper registration helpers, `createQueuebitCoordinator`, Coordinator owner/generation claim, source freeze/load dispatch, Batch-owned jobs, dispatchCursor CAS, and Coordinator tests.
- **Runtime M1C Batch checkpoint foundation**: added sequential batch barrier reconciliation, Redis `settle-batch` aggregation, checkpointCursor advancement, source exhausted Run execution terminal for `not_required` completion, and continuation tests.
- **Runtime M1D durable completion delivery**: added BatchRun `completion.batch/run` config, runtime completion handler registration, durable completion event hashes and indexes, `completions.get/list/retry`, Coordinator `deliverDueCompletions`, Batch/Run handler delivery fencing, failed-handler retry state, stale delivering lease recovery, and Node built-in tests for completion success, failure, retry, and generation conflicts.
- **Runtime M1E run controls**: added `runs.pause/resume/cancel`, run control timestamps and cancel reason snapshots, BatchRun-owned waiting job cancellation, Coordinator paused/pausing/cancelling convergence, immediate `run.cancelled` completion for idle runs, and an explicit `runs.retryFailed` guard until recovery replay is implemented.
- **Runtime M1F processor failure recovery**: added `runs.listFailures`, stable processor failure envelopes, payload-hidden failure pagination, parent-linked recovery Runs through `runs.retryFailed`, Coordinator Redis replay source for processor failures, and regression coverage for no duplicate failure envelopes before a whole Batch is terminal.
- **Runtime M1G mapper failure continuation**: added per-record mapper failure continuation, atomic mapper failure envelopes, payload-hidden mapper failure listing, mapper recovery Run replay without source reload, zero-job checkpoint advancement, and regression coverage for mapper recovery failures.
- **Runtime M1H real Redis harness**: added `npm run test:redis` and an environment-gated real Redis integration suite covering direct `addBulk`, multi-Worker processing, BatchRun source pagination, run completion delivery, processor failure envelopes, and recovery Run replay against a caller-supplied Redis `>=7.2` endpoint.
- **Runtime M1I CLI foundation**: added the packaged `queuebit` bin, Node 20 TypeScript config/runtime loader backed by the TypeScript compiler API, `.mjs/.js/.cjs` module loading, runtime processor registration, CLI JSON/human envelopes, exit-code mapping, scheduler unsupported rejection, basic config/run/job/completion/queue/worker/coordinator commands, and CLI spawn tests.
- **Runtime M1J role heartbeat and drain control**: added Redis-backed Worker/Coordinator role heartbeats, active/stale role inspection, remote drain requests, worker heartbeat auto-drain, coordinator loop heartbeat/drain handling, heartbeat interval/TTL config defaults, `client.roles`, and role registry tests.
- **Runtime M1K vext adapter**: added the `queuebit/vext` subpath, `createQueuebitVextPlugin`, optional `vextjs@0.3.26` peer/dev typing, Queuebit client logger option, vext plugin lifecycle tests, and a temporary consumer TypeScript smoke for native vext plugin integration.
- **Runtime M1L Redis fault harness**: added `npm run test:redis:faults` and an environment-gated Redis fault-injection suite for Worker crash simulation, stalled recovery, stale lease fencing, and second-Worker completion against a caller-supplied Redis `>=7.2` endpoint.
- **Runtime M1M Sentinel connection coverage**: added Redis `url`/direct/Sentinel connection mode normalization, official `@redis/client` `createSentinel` option mapping, Sentinel sendCommand adapter lifecycle coverage, `npm run test:redis:sentinel`, and an environment-gated Sentinel target smoke that preserves namespace-only cleanup.
- **Runtime M2A backpressure and paced dispatch foundation**: added public `QB_BACKPRESSURE_REJECTED` / `QB_BACKPRESSURE_REQUEST_TOO_LARGE` errors, queue jobs/bytes high-low latch fields, direct Producer backpressure rejection, Coordinator `dispatchHoldReason` persistence for `interval`, `in_flight_limit`, and `backpressure`, paced `intervalMs` / `maxInFlightBatches` gating, and tests that prove holds do not read source or advance cursors.
- **Runtime M2B retention, capacity, and observability foundation**: added normalized `retention`, `deduplication`, and `observability` config, ready-time TTL/timing guards, read-only `client.retention.plan()`, read-only `client.capacity.snapshot()`, metric prefix/disabled behavior, and tests for the non-destructive release-readiness foundation.
- **Runtime M2C out-of-order checkpoint and fan-out capacity foundation**: added `checkpointBatchIndex`, contiguous checkpoint advancement across settled Batch indexes, no premature terminal Run creation for exhausted empty pages behind earlier in-flight batches, settleCompletion key-count alignment for real Redis execution, a dispatch Lua block-balance fix, and tests for out-of-order paced batches, delayed batch completion delivery, empty tail pages, and one-to-many mapper capacity by actual jobs.
- **Runtime M2D observability backend foundation**: added the formal `src/observability` backend, shared in-process metrics registry, Prometheus rendering from the same samples, direct job submit counters, Worker claim/terminal/duration/attempt/stalled recovery metrics, role heartbeat/drain observation metrics, Coordinator advance/completion delivery counters, client facade guardrails that keep client-created Worker/Coordinator roles on the managed recorder, and targeted tests without adding an HTTP endpoint or new dependency.
- **Runtime M2E observability HTTP and alert foundation**: added no-listen `observabilityHttp.handle()` responses for health and Prometheus metrics, exported `createQueuebitObservabilityHttpApi()` for custom application-mounted paths, added `alerts.evaluate()` for local health/metrics/capacity findings, and covered GET/HEAD/404/405/custom-path/alert contracts without adding a server listener or dependency.
- **Runtime M2F retention purge foundation**: added safe-default `client.retention.purge()` with dry-run and execute modes, state-index based candidate discovery, Redis Lua guarded deletion for safe direct completed jobs, protected skip reasons for tombstone-bound/BatchRun-owned/non-terminal data, public purge result types, and targeted tests that verify dry-run does not mutate Redis while execute synchronizes job indexes and retained completed counters.
- **Runtime M2G Job tombstone foundation**: added direct completed Job `detailsExpired=true` tombstone compaction for deduplication/idempotency/replacement-bound identities, public `would_tombstone` / `tombstoned` purge decisions plus tombstoned counts, snapshot/list support for expired details without fabricating payload/result data, dedupe identity readback after retention, and targeted tests for delete/tombstone/conflict behavior.
- **Runtime M2H terminal Run tombstone foundation**: added age-based terminal Run `detailsExpired=true` tombstone compaction for delivered/not-required completions, failure envelope expiry with explicit `QB_RUN_STATE_CONFLICT` guards, `terminalRuns` retention purge candidates/results, `completion_protected` skip evidence, and targeted tests for dry-run/execute/readback/protected behavior.
- **Runtime M2I Completion event tombstone foundation**: added delivered/not-required Completion event detail compaction after the parent Run is terminal, preserving event identity and `summaryDigest` while clearing `summary`, retry/backoff/error, due, and delivery lease fields; `completions.get/list` read expired event tombstones, `completions.retry` rejects them with `QB_COMPLETION_STATE_CONFLICT`, and retention purge now reports `completions` candidates/results.
- **Runtime M2J terminal Run maxCount precision**: added a terminal Run detail index maintained by Run cancellation and Coordinator terminal transitions, made `retention.purge()` compute `terminalRuns.maxCount` from terminal full-detail records instead of the all-Run index, and removes tombstoned/stale members so non-terminal Runs no longer consume the terminal retention window.
